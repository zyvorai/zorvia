# KubeVirt VirtualMachine Manifest
# Generated by Zorvia
apiVersion: kubevirt.io/v1
kind: VirtualMachine
metadata:
  labels:
    kubevirt.io/vm: production-db
    os: ubuntu
    os.version: '22.04'
  name: production-db
  namespace: default
spec:
  running: false
  runStrategy: null
  template:
    metadata:
      labels:
        os: ubuntu
        os.version: '22.04'
    spec:
      domain:
        resources:
          requests:
            memory: 32Gi
        cpu:
          cores: 8
          sockets: 1
          threads: 1
        memory:
          guest: 32Gi
        devices:
          disks:
          - name: rootdisk
            disk:
              bus: virtio
            bootOrder: 1
          - name: datadisk
            disk:
              bus: virtio
            bootOrder: 2
          - name: cloudinitdisk
            disk:
              bus: virtio
          interfaces:
          - name: default
            model: virtio
            masquerade: {}
      volumes:
      - name: rootdisk
        containerDisk:
          image: quay.io/containerdisks/ubuntu:22.04
          imagePullPolicy: IfNotPresent
      - name: datadisk
        emptyDisk:
          capacity: 20Gi
      - name: cloudinitdisk
        cloudInitNoCloud:
          userData: |
            #cloud-config
            user: ubuntu
            password: ubuntu
            chpasswd: { expire: False }
            ssh_pwauth: True
            package_update: true
            packages:
              - qemu-guest-agent
            runcmd:
              - [ systemctl, enable, qemu-guest-agent ]
              - [ systemctl, start, qemu-guest-agent ]
      networks:
      - name: default
        pod: {}
      terminationGracePeriodSeconds: 30
